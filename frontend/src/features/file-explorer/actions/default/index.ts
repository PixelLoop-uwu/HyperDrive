import type { ModeConfig } from "../types";
import { defaultContextMenuConfig } from "./contextMenu.config";
import { defaultPathBarConfig } from "./pathBar.config";

export const defaultModeConfig: ModeConfig = {
  contextMenu: defaultContextMenuConfig,
  pathBar: defaultPathBarConfig,
};
